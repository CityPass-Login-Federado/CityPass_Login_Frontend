# Development: /api prefix is rewritten by Vite proxy to http://localhost:8081
# Production: replace with the actual API URL (e.g., https://api.citypass.local)
VITE_API_BASE_URL=/api
VITE_CLIENT_ID=citypass-admin-web
